#!/usr/bin/env bash

if [[ ! -f "${DATA_DIR}/makefiles/apply/Makefile" ]]; then
    cat << EOF
Error: file "${DATA_DIR}/makefiles/apply/Makefile" does not exist.

>>> Run 'tfmake makefile --apply' first."
EOF
    exit 1
fi

truncate -s 0 "${DATA_DIR}/outputs/apply/nodes"
truncate -s 0 "${DATA_DIR}/outputs/apply/visited"

make -f "${DATA_DIR}/makefiles/apply/Makefile"
