#!/usr/bin/env bash

if [[ ! -f "${DATA_DIR}/makefiles/plan/Makefile" ]]; then
    cat << EOF
Error: file "${DATA_DIR}/makefiles/plan/Makefile" does not exist.

>>> Run 'tfmake makefile --plan' first."
EOF
    exit 1
fi

truncate -s 0 "${DATA_DIR}/outputs/plan/nodes"
truncate -s 0 "${DATA_DIR}/outputs/plan/visited"

make -f "${DATA_DIR}/makefiles/plan/Makefile"
